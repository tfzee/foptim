config_version = 1
include = [
  "Default",
  # "FastMath",
]

[pipeline.fir.default_release]
passes = [
  "pipeline:default_release_early",
  "pipeline:default_release_middle",
  "pipeline:default_release_late",
  "pipeline:default_release_cleanup",
]

#########
# TOOD potential futures stuff


# conditional stuff
# [pipeline.fir.loopExtra]
# cond = [
#   "loopCount == 0"
# ]
# passes = [
#   "LICM", "LoopRotate", "LoopSimplify", "DCE",
#   "ConstLoopEval", "SimplifyCFG", "InstSimplify", "DCE",
#   "LoopUnswitch", "LoopUnroll", "DCE",
#   "SimplifyCFG", "InstSimplify", "DCE",
# ]
# [pass.FuncDedupLate]
# extends = "func_dedup"
# when = "is_hot"
# merge_partial_matches = true

# [[pass_override]]
# filter = "annotation:always_inline"
# pass = "inline"
# always_inline = true




# like include but gets applied afterwards
# overlay = [
#   "fastmath",
# ]
# [remarks]
# output = "remarks.yaml"
# passes = ["SLPVectorizer"]
# filter = "hot"
# missed = true


